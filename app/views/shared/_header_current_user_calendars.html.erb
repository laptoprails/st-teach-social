<li class="dropdown">
  <a href="#" class="dropdown-toggle" data-toggle="dropdown">calendars <b class="caret gray-caret"></b></a>
  <ul class="dropdown-menu">
  	<li class="dropdown-submenu pull-left">
  		My Courses
  		<ul class="dropdown-menu">
  			<% current_user.courses.each do |course| %>
        		<li><%= link_to "#{course.course_name}", unit_calendar_course_path(course) %></li>
    		<% end %>
  		</ul>
  	</li>
  </ul>
</li>