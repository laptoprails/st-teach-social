
  <%= yield :sidebar_mailchimp  %>

  <%= yield :sidebar_profile %>

  <%= render 'shared/sidebar_share_this' %>

  <% unless user_signed_in? %>
      <%= render 'shared/sidebar_sign_up' %>
  <% end %>
  
  <%= yield :sidebar_invite %>

  <%= yield :sidebar_other_user_courses %>

  <%= yield :sidebar_other_course_units %>

  <%= yield :sidebar_other_unit_lessons %>

  <%= yield :sidebar_related_courses %>

  <%= yield :sidebar_related_units %>

  <%= yield :sidebar_related_lessons %>

  <%= yield :sidebar_recent_questions %>



